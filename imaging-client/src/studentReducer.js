import axios from 'axios';

export const studentReducer = (state, action) => {
return state;
  // switch (action.type) {
  //   case 'GET_STUDENTS':

    
  //   default:
  //     return state;
  // }
}